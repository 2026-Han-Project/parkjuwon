# 버전을 == 로 고정한 이유
#   미고정 상태에서는 pip 가 항상 최신을 받아오기 때문에, 개발 PC에서 검증한 결과와
#   심사·발표 PC에서 나오는 결과가 달라진다. 실제로 statsmodels 가 0.15 에서
#   grangercausalitytests 의 verbose 인자를 제거해, 새로 설치한 환경에서는 H2' 탭의
#   Granger 인과검정이 TypeError 로 죽고 p-value 가 조용히 None 이 됐다.
#   아래 버전은 2026-09-04 기준 이 프로젝트에서 실제로 돌려 검증한 조합이다.
#   올릴 때는 반드시 H1/H2' 백테스트를 다시 돌려 폴백 알림이 뜨지 않는지 확인할 것.

streamlit==1.59.1
pandas==3.0.3
numpy==2.5.1
plotly==6.9.0
statsmodels==0.14.6
openpyxl==3.1.5

# 머신러닝 (탭1 앙상블 · 탭5 이상탐지 · 탭6 SHAP)
scikit-learn==1.9.0
xgboost==3.3.0
shap==0.52.0

# 시계열 기저모델 (Layer 1) · H1/H2' 회귀변수 투입
prophet==1.3.0

# TFT 기저모델 비교 (논문용). torch/lightning 은 pytorch_forecasting 이 끌어오지만,
# 예측값이 이 두 버전에 따라 달라지므로 재현성을 위해 함께 고정한다.
pytorch_forecasting==1.8.0
torch==2.13.0
lightning==2.6.5

# 에이전트 브리핑 파이프라인 (조회 -> 예측 -> 해석 -> 브리핑)
langgraph==1.2.11

# 선택 기능 - 탭1 LLM 브리핑. 미설치여도 앱은 정상 동작하며,
# API 키가 없으면 호출 경로 자체가 비활성이라 비용이 발생하지 않는다.
anthropic==1.2.0
