# 팀 통합 주의사항 (B파트 → 통합 시)

2026-09-04 기준. 통합 담당자가 **코드를 합치기 전에** 읽어야 하는 항목만 모았다.
각 항목은 실제로 재현해 확인한 것이고, 확인 방법도 함께 적었다.

---

## 1. 실행 스크립트에서 `PYTHONUTF8` 을 설정하면 안 된다 (가장 중요)

한글 Windows에서 `PYTHONUTF8=1` 이 켜져 있으면 **Prophet 이 100% 죽는다.**

```
cmdstanpy 가 where.exe 로 tbb.dll 을 찾는다
  -> where.exe 가 CP949 한글 문자열을 출력한다
     (찾기 실패 메시지든, 한글이 든 경로든 마찬가지)
  -> Python UTF-8 모드라 그 출력을 UTF-8 로 디코드하다 UnicodeDecodeError
  -> cmdstanpy 는 RuntimeError 만 잡으므로 예외가 그대로 올라간다
  -> Prophet 은 백엔드 로딩 실패를 조용히 삼키고
     "'Prophet' object has no attribute 'stan_backend'" 라는 엉뚱한 에러를 낸다
  -> predict_prophet / predict_prophet_with_regressors 가 전부 선형추세로 폴백
```

결과가 나쁜 게 아니라 **의미가 없어진다.** H1 탭의 기저 예측과 이벤트 회귀가 완전히
같은 값이 되어 개선폭이 전 품목 `+0.0%p` 로 나온다. 가설이 기각된 것처럼 보이지만
사실은 Prophet 이 한 번도 돌지 않은 것이다.

- 이 저장소의 `실행하기.bat` 은 `chcp 65001` 만 하고 `PYTHONUTF8` 은 건드리지 않는다. **안전하다.**
- `2026-Han-Project/Biz-Forecaster` 의 `실행하기.bat` 은 `set "PYTHONUTF8=1"` 과
  `set "PYTHONIOENCODING=utf-8"` 을 설정한다. **그 런처를 통합본에 그대로 쓰면 안 된다.**
- 폴더 경로에 한글이 없으면 증상이 안 나타날 수도 있어서, 사람마다 결과가 달라지는
  형태로 나타난다. "제 PC에서는 개선폭이 나오는데요" 가 여기서 생긴다.

`app.py` 에 시작 시 백엔드 건강검진(`check_prophet_backend()`)을 넣어두었다. 이 상황이면
화면 맨 위에 원인과 해법이 빨간 박스로 뜨고 `PROPHET_AVAILABLE` 이 내려간다.

**확인 방법**

```bat
.venv\Scripts\python.exe -c "from prophet import Prophet; Prophet(); print('OK')"
```

### 1-2. `docker compose` 도 한글 폴더명에서 실패한다

같은 뿌리(한글 경로)에서 나오는 별개의 증상이다. Grafana 스택을 띄울 때

```
docker compose up -d
  -> error: project name must not be empty
```

Docker Compose 는 프로젝트 이름을 **폴더명에서 자동으로 만드는데**, 폴더명이
`수요예측프로그램` 처럼 전부 비ASCII 문자면 허용 문자만 걸러낸 결과가 빈 문자열이 되어
거부된다. 폴더를 옮기지 않으려면 프로젝트명을 직접 주면 된다.

```bat
docker compose -p biz-forecaster up -d
docker compose -p biz-forecaster down
```

`-p` 를 빼먹으면 컨테이너를 찾지도 내리지도 못하므로, **올릴 때와 내릴 때 같은 이름을
써야 한다.** 프로젝트 폴더를 영문 경로로 옮기면 1절의 Prophet 문제와 함께 사라진다.

---

## 2. `requirements.txt` 버전을 함부로 풀지 말 것

버전을 `==` 로 고정해 두었다. 미고정 상태에서는 pip 가 항상 최신을 받아오기 때문에
개발 PC와 발표 PC의 결과가 달라진다. 실제로 겪은 사례:

- `statsmodels` 0.15 에서 `grangercausalitytests()` 의 `verbose` 인자가 제거됐다.
  기존 코드는 `verbose=False` 를 넘기고 있어서 새 환경에서는 `TypeError` 가 나고,
  그걸 `except` 가 삼켜 **p-value 가 조용히 `None`** 이 됐다. H2' 인과성 검정이 통째로
  무효인데 화면에는 "유의하지 않음" 처럼 보인다.
  → 인자를 아예 넘기지 않도록 고쳤다(0.14 / 0.15 양쪽 호환).

버전을 올릴 때는 **반드시 H1 · H2' 백테스트를 다시 돌려 폴백 알림이 뜨지 않는지 확인**할 것.

---

## 3. 빨간 "폴백 발생" 알림이 뜨면 그 숫자는 보고서에 쓰지 말 것

예측 함수들은 실패해도 예외를 올리지 않고 더 단순한 모델로 갈아탄다. 앱이 죽지 않게 하는
안전장치라 유지해야 하지만, 그게 조용하면 백테스트 숫자가 무엇을 측정한 건지 알 수 없다.

계측을 넣어 폴백 7개 함수 12곳을 기록하고, 탭1 · 탭3(H1) · 탭4(H2') 결과 위에 발생 건수와
사유를 띄운다. 폴백이 없으면 아무것도 그리지 않으므로, **알림이 안 뜨는 것이 정상 상태**다.

특히 위험한 두 가지:

| 폴백 | 그러면 무슨 일이 생기나 |
|---|---|
| `predict_prophet_with_regressors` → `predict_prophet` | 외생변수가 빠져서 '기저 vs 이벤트 회귀' 가 같은 값끼리의 비교가 된다 |
| `predict_tft` → `predict_prophet` | 'TFT vs Prophet' 비교가 Prophet vs Prophet 이 된다 |

---

## 4. 데이터셋 스냅샷이 서로 다르다 — 수치를 직접 비교하면 안 된다

| 저장소 | `integrated_dataset.csv` | 행 수(헤더 포함) |
|---|---|---|
| `2026-Han-Project/dataset` (A파트) | md5 `56a1637c82dc4f80d7fa2efd4cca6d45` | 71,585 |
| `2026-Han-Project/parkjuwon` (B파트) | 위와 **완전 일치** | 71,585 |
| `2026-Han-Project/Biz-Forecaster` (C파트) | md5 `161969ea3c084dfc9265bc4185830b1f` | 71,883 |

스키마(15개 컬럼)와 기간(2024-01-01 ~ 2025-12-31)은 같지만 판매량 값 자체가 다르다.
합성 데이터를 다시 생성한 결과로 보인다. **A파트 최신본으로 통일한 뒤 모든 수치를 재측정**해야
한다. 그 전까지 B파트 WAPE 와 C파트 오탐/미탐률을 나란히 놓으면 안 된다.

---

## 5. 통합 전에 팀이 결정해야 할 것

| 항목 | B파트 (parkjuwon) | C파트 (Biz-Forecaster) | 비고 |
|---|---|---|---|
| LLM 제공자 | Anthropic (`ANTHROPIC_API_KEY`) | OpenAI (`gpt-4o-mini`) | 하나로 통일 필요 |
| SHAP | 실제 SHAP | LLM 브리핑에는 `agent/mock_shap.py`(해시 기반 가짜값) | 실제 SHAP 으로 연결 권장 |
| 파일 인코딩 | UTF-8 | CP949 | 섞이면 `설명서.txt` 가 깨진다 |
| 데이터 로더 | POS 영수증 텍스트 파서 | 통합 CSV 표준 로더 | 양쪽 다 받도록 합치는 것이 안전 |

**모듈 분리 관련**: C파트 저장소는 이미 `agent/` · `detect/` · `auth/` 로 나뉘어 있고
B파트 예측 함수 25개를 바이트 단위로 동일하게 포함하고 있다. 통합 베이스는 C파트 구조로
잡고, 그 위에 B파트의 H2' 품목연쇄 · Grafana 스택 · Prophet `add_regressor` 리팩터를
얹는 편이 재작업이 가장 적다. **그 방향이 정해지기 전에 B파트에서 `app.py` 를 따로 쪼개면
병합 충돌만 커진다.**

---

## 6. 재현용 기준값 (2026-09-04, A파트 데이터셋 기준)

버전을 올리거나 데이터셋을 바꾼 뒤에는 아래 값이 재현되는지 확인한다.
**모든 항목에서 폴백 0건**이어야 한다.

**H1 — 이벤트 회귀 (상위 4품목, 검증 14일)**

| 품목 | 기저 WAPE | 이벤트 회귀 WAPE | 차이 |
|---|---|---|---|
| 소보로빵 | 21.1% | 20.6% | −0.5%p |
| 단팥빵 | 18.8% | 19.2% | +0.4%p |
| 아메리카노 | 27.8% | 26.3% | −1.5%p |
| 크림빵 | 18.6% | 17.8% | −0.7%p |

**H2' — 품목간 수요연쇄 (7개 카테고리 42쌍 전수, 검증 14일)**

- 폴백 0건, Granger p-value 42개가 모두 실수로 반환됨 (`None` 0건)
- 최적 시차는 대부분 7일 또는 14일, 교차상관 r = +0.30 ~ +0.60
- 연쇄 회귀의 기저 대비 개선폭은 −0.9%p ~ +0.4%p 로 대체로 미미하다.
  두 카테고리가 요일 · 계절이라는 공통 요인을 공유해서 기저모델이 이미 그 변동을
  설명하고 있기 때문이다. **이 결과 자체는 정상이며 폴백 때문이 아니다.**

---

## 7. 아직 안 고친 것

- `load_data()` 가 POS 영수증 텍스트만 파싱한다. 통합 CSV 업로드는 별도 경로(`load_event_dataset`)를 쓴다.
- `.env.example` 에 이 저장소가 쓰지 않는 키(`KMA_SERVICE_KEY`, `NEIS_API_KEY`,
  `GOOGLE_CALENDAR_ID`)가 남아 있다. 해당 파이프라인은 `2026-Han-Project/dataset` 에 있다.
- `tensorflow` 는 `requirements.txt` 에 없다. 따라서 `predict_lstm` 은 항상 비활성이고
  앙상블은 README 표의 6개가 아니라 5개 모델로 돈다. 코드는 `if TF_AVAILABLE` 로 제대로
  막고 있어 버그는 아니지만, 발표 때 모델 개수를 말할 때 주의할 것.
